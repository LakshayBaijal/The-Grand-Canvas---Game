# Grand Canvas settings. Copy this file to ".env" (same folder) and fill it in.
# ".env" is ignored by git, so your real values never get pushed.
#
# Who reads what:
#   the server (start-server.bat / start-server.sh, and the deployed server)
#     reads PORT, DB_PATH, GOOGLE_CLIENT_ID
#   build-app.ps1 (builds the APK / app bundle for the Play Store)
#     reads SERVER, GOOGLE_SERVER_CLIENT_ID, ADMOB_BANNER_ID, ADMOB_REWARDED_ID,
#     PASS_PRODUCT_ID; and Gradle reads ADMOB_APP_ID for the manifest
#   the "Links" section is only for you, so everything is in one place.

# ---- Server -----------------------------------------------------------------

# Port the server listens on. Leave 8090 unless something else is using it.
PORT=8090

# Where the SQLite database file lives. Players, drawings, hearts, hall of
# fame: everything is in this one file. On the server this is an absolute
# path on its disk (setup-vm.sh sets it); on your laptop the default is fine.
DB_PATH=data/leaderboard.db

# Google sign-in (optional). The *Web* OAuth client id from Google Cloud
# Console -> APIs & Services -> Credentials. Looks like
# 123456789012-abcdefghijklmnop.apps.googleusercontent.com
# Leave empty and the game runs on device accounts only.
GOOGLE_CLIENT_ID=

# ---- App build --------------------------------------------------------------

# Where the app connects. A bare host name means secure (wss://) automatically;
# a bare IP or "localhost" means plain ws:// (for playing on your own Wi-Fi).
#   your server's name: grandcanvas.duckdns.org
#   your laptop:      192.168.1.10:8090
SERVER=

# The same Web client id as GOOGLE_CLIENT_ID above. The app sends it to
# Google so the token it gets back is one this server will accept.
GOOGLE_SERVER_CLIENT_ID=

# ---- Ads and the pass (AdMob + Google Play Billing) ------------------------
# All optional. Left empty, the app shows Google's *test* ads (real-looking,
# earn nothing) and the pass can't be bought. Fill these in from your AdMob
# account (apps.admob.com) and the Play Console once the app exists there.

# AdMob -> Apps -> your app -> App settings -> "App ID". Has a ~ in it.
# Goes into the Android manifest at build time.
ADMOB_APP_ID=

# AdMob -> Apps -> your app -> Ad units. One "Banner" unit and one
# "Rewarded" unit. Each has a / in it.
ADMOB_BANNER_ID=
ADMOB_REWARDED_ID=

# Play Console -> Monetize -> Products -> In-app products. Create one,
# product id exactly this, type one-time (non-consumable), price 99 INR.
PASS_PRODUCT_ID=grand_canvas_pass

# ---- Links (for you; nothing reads these) -----------------------------------

# Where the server is rented (DigitalOcean, Bangalore), and its public IP.
HOSTING_CONSOLE_URL=https://cloud.digitalocean.com/
VM_PUBLIC_IP=
# The SSH key that gets you into the server (the private key file on your laptop).
SSH_KEY_PATH=

# DuckDNS (free domain name for the server). Your account page and your token.
DUCKDNS_URL=https://www.duckdns.org/domains
DUCKDNS_DOMAIN=
DUCKDNS_TOKEN=

# Google Cloud project for sign-in.
GOOGLE_CLOUD_CREDENTIALS_URL=https://console.cloud.google.com/apis/credentials

# Google Play Console.
PLAY_CONSOLE_URL=https://play.google.com/console/

# AdMob (ads) — where the money shows up.
ADMOB_URL=https://apps.admob.com/

# The upload keystore for signing the app. NEVER lose this file or its
# passwords; keep a copy somewhere that isn't this laptop.
KEYSTORE_PATH=
KEYSTORE_BACKUP_LOCATION=

# The GitHub repo.
GITHUB_REPO_URL=https://github.com/LakshayBaijal/The-Grand-Canvas---Game
